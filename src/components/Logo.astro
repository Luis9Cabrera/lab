---
interface Props {
  variant?: "nav" | "footer";
  className?: string;
}

const { variant = "nav", className = "" } = Astro.props;
---

<div class={`flex items-center gap-3 group ${className}`}>
  <div
    class={`relative transition-all duration-300 group-hover:scale-105 ${
      variant === "nav" ? "w-40 h-auto" : "w-48 h-auto"
    }`}
  >
    <img
      src={`${import.meta.env.BASE_URL}/logo-ven.svg`}
      alt="Logo Venecia Laboratorio Clínico"
      class="w-full h-full object-contain"
      loading="eager"
      width={120}
      height={80}
    />
  </div>
</div>
