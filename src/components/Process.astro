---
import { CalendarRange, Pipette, Microscope, CheckCircle } from "lucide-astro";

const steps = [
  {
    title: "Agendar",
    desc: "Reserva tu cita vía WhatsApp o teléfono en segundos.",
    icon: CalendarRange,
  },
  {
    title: "Toma de Muestra",
    desc: "Visítanos o solicita servicio a domicilio.",
    icon: Pipette,
  },
  {
    title: "Procesamiento",
    desc: "Análisis con tecnología de vanguardia.",
    icon: Microscope,
  },
  {
    title: "Resultados",
    desc: "Descarga tus resultados en línea o recíbelos por WhatsApp.",
    icon: CheckCircle,
  },
];
---

<section id="proceso" class="py-24 bg-primary text-white overflow-hidden">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-5xl font-bold mb-6">Tu proceso es simple</h2>
      <p class="text-blue-100 italic">
        Rapidez y claridad en cada paso de tu camino a la salud.
      </p>
    </div>

    <div class="grid md:grid-cols-4 gap-8 relative">
      <!-- Decorative connector line -->
      <div
        class="hidden md:block absolute top-12 left-0 w-full h-0.5 bg-secondary/30 -z-0"
      >
      </div>

      {
        steps.map((step, index) => (
          <div class="flex flex-col items-center text-center relative z-10">
            <div class="w-24 h-24 rounded-full bg-secondary flex items-center justify-center mb-8 shadow-2xl group border-4 border-primary ring-8 ring-primary/20 transition-transform hover:scale-110">
              <step.icon class="w-10 h-10 text-white" />
              <span class="absolute -top-2 -right-2 w-8 h-8 rounded-full bg-white text-primary font-black flex items-center justify-center shadow-lg">
                {index + 1}
              </span>
            </div>
            <h3 class="text-xl font-bold mb-4">{step.title}</h3>
            <p class="text-blue-100/80 leading-relaxed px-4">{step.desc}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
